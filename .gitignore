# Node
node_modules
npm-debug.log*

# Local env
.env

# Local Docker test data (may contain credentials after setup)
.tmpdata

# Build artifacts
/dist

# Backup exports
*.tar.gz

# Personal Tailscale ACL configs (contain real emails/tags — keep local only)
access controls.json

# OS
.DS_Store
